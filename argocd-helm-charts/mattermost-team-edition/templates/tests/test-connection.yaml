apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mattermost-team-edition.fullname" . }}-test-connection"
  labels:
    {{- include "mattermost-team-edition.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mattermost-team-edition.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
