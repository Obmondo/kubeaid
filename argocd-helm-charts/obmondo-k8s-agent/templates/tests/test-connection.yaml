apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "obmondo-k8s-agent.fullname" . }}-test-connection"
  labels:
    {{- include "obmondo-k8s-agent.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "obmondo-k8s-agent.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
