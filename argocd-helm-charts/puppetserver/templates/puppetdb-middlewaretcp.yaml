apiVersion: traefik.io/v1alpha1
kind: MiddlewareTCP
metadata:
  name: middlewaretcp-{{ .Values.customerid }}
spec:
  ipAllowList:
    sourceRange:
      {{- range .Values.ipAllowedList }}
      - "{{ . }}"
      {{- end }}
