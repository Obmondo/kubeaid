apiVersion: trust.cert-manager.io/v1alpha1
kind: Bundle
metadata:
  name: step-ca-bundle
spec:
  sources:
  - useDefaultCAs: true
  - configMap:
      name: "step-ca-step-certificates-certs"
      key: "root_ca.crt"
  target:
    namespaceSelector: {}
    secret:
      key: "ca.crt"
